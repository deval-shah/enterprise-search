stages:
  pull_test_data:
    cmd: dvc pull --force ../data/test_docs.dvc
  run_tests:
    cmd: pytest
    wdir: ..
    deps:
      - ../data/test_docs
      - .
    outs:
      - ../report.html:
          cache: false
